# For npm (coming soon - scope setup pending)
# npm install -g @gorlomi/mcphosting-cli

# From GitHub (recommended - works now)
npm install -g gorlomi-enzo/mcphosting-cli

# Or clone and install locally
git clone https://github.com/gorlomi-enzo/mcphosting-cli
cd mcphosting-cli
npm install
npm run build
npm link
